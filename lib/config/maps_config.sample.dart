// Sample config for maps-related API keys.
// Copy this file to `lib/config/maps_config.dart` and insert your
// real key locally. The real file is git-ignored.

const String googleStaticMapsApiKey = '';
